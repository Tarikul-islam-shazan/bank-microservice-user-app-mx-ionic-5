import { SuppressDirective } from './suppress.directive';

describe('SuppressDirective', () => {
  it('should create an instance', () => {
    const directive = new SuppressDirective();
    expect(directive).toBeTruthy();
  });
});
