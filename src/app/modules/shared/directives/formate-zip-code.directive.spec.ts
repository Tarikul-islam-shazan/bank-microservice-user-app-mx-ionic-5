import { FormateZipCodeDirective } from './formate-zip-code.directive';

describe('FormateZipCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new FormateZipCodeDirective();
    expect(directive).toBeTruthy();
  });
});
